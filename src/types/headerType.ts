export interface LocalStorageKeywordType {
  categoryId: number
  text: string
}

export interface NotificationType {
  id: number
  code: string
  title: string
  content: string
  created_at: string
}
